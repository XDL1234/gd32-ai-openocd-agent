使用 `embedded-dev` skill 进行这次审查，因为任务直接针对该协议本身的工具调用规范。我先读取 `SKILL.md`、`refs/mcp-tools.md` 和策略计划文档，核对三工具的优先级、降级链和触发时机。目录已确认，接下来读取三份文件的相关段落并带上行号。重点先抓 `SKILL.md` 的 RESEARCH 1/7、INNOVATE 1 和工具规范，再对照计划文档与 `mcp-tools.md`。`SKILL.md` 和 `mcp-tools.md` 的关键条目已经定位到。现在单独读取策略计划文档，确认它是否把 `grok-search`、`Context7`、`gh CLI` 的角色定义得与主协议完全一致。已经定位到疑点比较集中的几段，但还需要原文确认措辞，否则无法判断“主工具”“并行调用”“降级到推理工具”之间是不是只是在表述层面冲突。下一步我用更原始的带行号读取方式抓具体段落。关键冲突已经浮出来了，主要集中在三个点：`Context7` 被拿去做引脚/手册查询、`grok-search` 的“主搜索工具”地位在入口文案里不稳定、以及主协议没有把 `gh CLI` 纳入可用性检测和降级矩阵。我再补抓策略计划里各工具的自我定位段落，避免遗漏。## 1. 发现的逻辑冲突或不一致\n\n- `[高]` `Context7` 的工具边界前后冲突。`refs/mcp-tools.md` 明确把它定义为“固件库 API / 函数签名 / 初始化顺序”查询工具，并说明“不擅长硬件原理和芯片引脚物理特性（数据手册内容）”；但 `SKILL.md` 和策略计划又把它放在“引脚复用/冲突分析”甚至“数据手册查询”入口位置，角色越界明显。参见 [mcp-tools.md:54](/C:/Users/A/.claude/skills/embedded-dev/refs/mcp-tools.md#L54) [mcp-tools.md:30](/C:/Users/A/.claude/skills/embedded-dev/refs/mcp-tools.md#L30) [SKILL.md:78](/C:/Users/A/.claude/skills/embedded-dev/SKILL.md#L78) [SKILL.md:214](/C:/Users/A/.claude/skills/embedded-dev/SKILL.md#L214) [tools-strategy-plan.md:176](/C:/tmp/tools-strategy-plan.md#L176)\n\n- `[高]` RESEARCH 步骤 7 的降级逻辑自相矛盾。步骤正文写的是“Context7 超时则跳过，使用默认引脚，PLAN 阶段再补验证”，但后面的容灾规范和策略计划写的是“立刻降级到 grok-search，再用 Sequential Thinking 辅助分析”。“默认引脚”与“禁止盲猜”直接冲突，风险最高。参见 [SKILL.md:214](/C:/Users/A/.claude/skills/embedded-dev/SKILL.md#L214) [SKILL.md:220](/C:/Users/A/.claude/skills/embedded-dev/SKILL.md#L220) [SKILL.md:441](/C:/Users/A/.claude/skills/embedded-dev/SKILL.md#L441) [tools-strategy-plan.md:226](/C:/tmp/tools-strategy-plan.md#L226)\n\n- `[中]` 主协议的容灾矩阵没有把三工具补齐。`gh CLI` 在计划和 `mcp-tools.md` 中有明确失败降级，但 `SKILL.md` 的“工具可用性检测”和“降级策略矩阵”只覆盖了 `Context7`、`grok-search` 等，缺少 `gh CLI`。主协议层缺口会导致实际执行时没有统一 fallback。参见 [SKILL.md:434](/C:/Users/A/.claude/skills/embedded-dev/SKILL.md#L434) [SKILL.md:459](/C:/Users/A/.claude/skills/embedded-dev/SKILL.md#L459) [mcp-tools.md:92](/C:/Users/A/.claude/skills/embedded-dev/refs/mcp-tools.md#L92) [tools-strategy-plan.md:289](/C:/tmp/tools-strategy-plan.md#L289)\n\n- `[中]` `grok-search` 的“主搜索工具”定位在入口文案里不稳定。辅助工具规范把它定义成“所有需要联网检索场景”的主工具，但快速工具索引对“搜索开源驱动”写成“`gh` CLI 或 grok-search”，对“数据手册查询”甚至写成“查手册 mode 或 Context7 MCP”，会让执行者绕开 grok。参见 [SKILL.md:75](/C:/Users/A/.claude/skills/embedded-dev/SKILL.md#L75) [SKILL.md:78](/C:/Users/A/.claude/skills/embedded-dev/SKILL.md#L78) [SKILL.md:386](/C:/Users/A/.claude/skills/embedded-dev/SKILL.md#L386) [tools-strategy-plan.md:96](/C:/tmp/tools-strategy-plan.md#L96)\n\n- `[中]` 策略计划绕过了主协议的“离线优先”链路。`SKILL.md` RESEARCH 步骤 1 明确要求先查离线索引，再查 EmbedSummary README，最后才扩展到 `gh`/`grok-search`；但计划中的实战示例直接从 `grok-search + gh CLI` 开始。这会造成不必要的重复查询，也和“避免重复搜索”原则不一致。参见 [SKILL.md:199](/C:/Users/A/.claude/skills/embedded-dev/SKILL.md#L199) [SKILL.md:201](/C:/Users/A/.claude/skills/embedded-dev/SKILL.md#L201) [SKILL.md:422](/C:/Users/A/.claude/skills/embedded-dev/SKILL.md#L422) [tools-strategy-plan.md:14](/C:/tmp/tools-strategy-plan.md#L14) [tools-strategy-plan.md:191](/C:/tmp/tools-strategy-plan.md#L191)\n\n- `[中]` `Sequential Thinking` 被当成 `grok-search` 的首降级工具，不等价。它能辅助推理，但不能替代“最新版本、GitHub 项目、数据手册链接、官方页面”这类事实检索；当前写法容易让“有推理、无证据”的结果通过。参见 [SKILL.md:386](/C:/Users/A/.claude/skills/embedded-dev/SKILL.md#L386) [SKILL.md:450](/C:/Users/A/.claude/skills/embedded-dev/SKILL.md#L450) [mcp-tools.md:49](/C:/Users/A/.claude/skills/embedded-dev/refs/mcp-tools.md#L49) [tools-strategy-plan.md:287](/C:/tmp/tools-strategy-plan.md#L287)\n\n- `[低]` `grok-search` 在 RESEARCH 步骤 4/5 的触发没有写进主协议步骤本体，只出现在 `mcp-tools.md` 和策略计划里，存在遗漏调用风险。参见 [SKILL.md:209](/C:/Users/A/.claude/skills/embedded-dev/SKILL.md#L209) [SKILL.md:210](/C:/Users/A/.claude/skills/embedded-dev/SKILL.md#L210) [tools-strategy-plan.md:114](/C:/tmp/tools-strategy-plan.md#L114)\n\n- `[低]` 恢复机制表述不一致。`SKILL.md` 写“每执行 3-5 个任务后检查一次”，策略计划写“每 5 次查询后尝试恢复”，粒度不同。参见 [SKILL.md:512](/C:/Users/A/.claude/skills/embedded-dev/SKILL.md#L512) [tools-strategy-plan.md:316](/C:/tmp/tools-strategy-plan.md#L316)\n\n## 2. 优先级设计的改进建议\n\n- 把 `Context7` 严格收敛为“固件库/API 文档工具”。优先级应改成：本地离线 refs > `Context7` > `grok-search` 定向搜官方文档。不要再把它写成引脚物理特性、数据手册、电气参数的第一选择。\n- 把“引脚复用/冲突分析”拆成两段：事实获取用官方数据手册/厂商 pinout 文档，方案推理用 `Sequential Thinking`。`Context7` 只在确有 SDK/驱动 API 关联时补充。\n- 把“开源驱动发现”和“GitHub 深挖”分层。发现阶段：离线索引/EmbedSummary 后用 `grok-search` 扩面；候选仓库一旦确定，再交给 `gh CLI` 做 repo view / code search / file read。\n- 把入口文案统一成一句话规则：官方 API 查 `Context7`，GitHub 仓库深查 `gh CLI`，其余联网检索优先 `grok-search`。现在“或”“最高优先级”并存，容易冲突。\n- 对“数据手册查询”改成：`grok-search` 负责找官方链接，Document Skills / Read 负责读内容，`Context7` 不再出现在这一入口。\n\n## 3. 容灾策略的补充建议\n\n- 在 `SKILL.md` 主协议层补齐三工具完整矩阵，至少显式加入 `gh CLI`：`gh CLI` 失败时先 `grok-search site:github.com`，再 GitHub 网页，最后才人工。\n- 调整 `grok-search` 的降级顺序。对事实检索类任务应先 `WebSearch/手动网页`，`Sequential Thinking` 只能做“已有证据的整理与比较”，不应做第一替代。\n- 给 `Context7` 增加“本地 refs / 官方 PDF”备份，而不只是 `grok-search`。尤其 STM32 HAL/StdPeriph 场景，`SKILL.md` 已有本地 refs，应写进统一降级链。\n- 明确禁止“使用默认引脚作为 Context7 失败后的临时结论”。失败后只能进入“官方文档搜索 -> 结果验证 -> 再规划”，不能先假设再补证据。\n- 恢复机制建议改成“每次工具失败后的下一个同类任务先探测主工具是否恢复”，比固定“每 5 次”更可执行，也不会漏掉短会话。\n\n## 4. 调用时机的优化建议\n\n- RESEARCH 步骤 1 应显式写成“离线索引 -> EmbedSummary -> `grok-search` 扩面 -> `gh CLI` 深查候选仓库”，避免计划示例一上来就并行联网。\n- RESEARCH 步骤 4/5 建议补一句条件触发：一旦出现报错、外设无响应、时序异常，必须优先 `grok-search` 检索现象和已知问题。\n- RESEARCH 步骤 7 建议改为“官方 pinout/手册获取 -> 冲突分析 -> 生成方案”，不要直接把 `Context7` 作为第一动作。\n- INNOVATE 步骤 1 可保留 `grok-search + gh CLI`，但要增加条件分流：需要找更多候选时先 `grok-search`，已有候选要评估质量时先 `gh CLI`。\n- 实战场景建议补 3 个示例，否则覆盖不完整：RESEARCH 步骤 6 的 API 模式记录、EXECUTE 阶段编译/链接错误排查、数据手册检索与读取联动流程。\n\n## 5. 综合评分（0-100）\n\n**71/100**\n\n扣分原因：\n- `Context7` 角色定义和实际使用范围冲突，且落在高风险的引脚/手册场景。\n- 主协议没有完整承接三工具容灾链，`gh CLI` 在主协议层缺失。\n- `grok-search` 的主搜索定位没有在入口层统一，导致触发时机不稳定。\n- 降级链里把 `Sequential Thinking` 当成事实搜索替代，可靠性不足。\n- 实战示例没有完全覆盖主协议里的典型场景，尤其 RESEARCH 6 / EXECUTE / 数据手册工作流。\n\n## 6. 关键改进优先级\n\n1. 立即重定义 `Context7` 的边界：只保留 API/固件库查询，移出“数据手册/引脚物理特性/默认引脚”相关表述。\n2. 在 `SKILL.md` 主协议层补齐三工具统一容灾链，并删除“Context7 失败就先用默认引脚”的危险降级。\n3. 统一入口规则和 RESEARCH/INNOVATE 流程：离线优先，`grok-search` 负责广域发现，`gh CLI` 负责 GitHub 深查，避免“或”“并行”“主工具”三套说法并存。
